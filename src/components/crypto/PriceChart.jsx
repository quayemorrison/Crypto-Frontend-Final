const PriceChart = ({ data, className = "" }) => {
    return (
        <div className={`bg-gray-50 rounded-lg p-4 h-64 flex items-center justify-center border border-gray-200 ${className}`}>
            <p className="text-gray-400">Chart placeholder. Will render chart here.</p>
        </div>
    );
};

export default PriceChart;
